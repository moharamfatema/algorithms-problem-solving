#ifndef HEADER_HPP
#define HEADER_HPP
#include <vector>
#include <math.h>
#include <random>

class Utils
{
    public:
    static int max(std::vector<int> arr);
    static int noOfDigits(int a);
    static void swap (int &a, int&b);
};
#endif
