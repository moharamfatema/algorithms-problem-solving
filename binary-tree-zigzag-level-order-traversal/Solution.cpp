#include "Solution.hpp"

void Solution::breadth_first_search(std::queue<std::queue<TreeNode *>>& q,TreeNode* root, std::vector<std::vector<int>>& v)
{
    while(!q.empty())
    {
        
    }
}

std::vector<std::vector<int>> Solution::zigzagLevelOrder(TreeNode* root)
{
    
}